on:
  push:
    branches:
      - main
  pull_request:

name: CI

env:
  RUSTFLAGS: -Dwarnings

jobs:
  rust:
    runs-on: ubuntu-latest
    defaults:
      run: 
        working-directory: jjb
    steps:
      - uses: actions/checkout@v6
      - uses: Swatinem/rust-cache@v2
      - run: |
          cargo fmt -- --check
          cargo check
          cargo clippy
          cargo test
